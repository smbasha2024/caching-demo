export interface UserData {
  name: string;
  email: string;
  bio: string;
  timestamp: string;
  source: string;
}


export interface FileData {
  fileData: string;
}

export interface TabConfig {
  id: string;
  label: string;
}